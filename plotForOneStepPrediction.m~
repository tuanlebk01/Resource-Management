close all;
clear
autoRetrainWithThreholdANFIS;
predicted1 = predictedV;
autoRetrainWithThreholdNARX;
predicted2 = 
figure(1)
subplot(1,1,1);
plot(predicted1);
hold on;
plot(originalData(index));
hold off
legend('NARX','Actual values')
subplot(1,1,2);
predictedV2 = cell2mat(ys(1:end));
plot(predictedV2);
hold on
plot(actualV); 
legend('NARX','Actual values')
hold off